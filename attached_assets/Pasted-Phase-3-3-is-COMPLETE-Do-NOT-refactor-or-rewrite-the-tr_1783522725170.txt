Phase 3.3 is COMPLETE.

Do NOT refactor or rewrite the tracer.

The backend is currently passing Phase 3.3.

Your task is ONLY to verify correctness and clean up the implementation without changing external behavior.

Requirements:

1. Run

python backend/test_phase32.py

and

python backend/test_phase33.py

Both MUST pass.

If either test fails:
- identify the exact failing assertion
- make the MINIMUM possible fix
- rerun only the failing test
- finally rerun both Phase 3.2 and Phase 3.3

Do NOT refactor unrelated code.

--------------------------------------------

2. Remove temporary debugging code ONLY

Search the backend for:

print(
logging.debug
debug_
TODO
TEMP
DEBUG

Remove ONLY temporary debugging statements.

Do NOT remove actual exception handling.

--------------------------------------------

3. Verify heap serialization

Check that:

- identical objects always share the same heap id
- nested objects serialize correctly
- circular references work
- heap snapshots are immutable
- primitives never go into the heap
- heap ids remain stable during one execution

Do NOT rewrite the serializer if these already work.

--------------------------------------------

4. Inspect for dead code

Only report:

- duplicate helper functions
- unreachable code
- unused imports
- unused private methods

DO NOT remove anything automatically.

Instead produce a report:

Dead Code Report
----------------
File:
Reason:
Safe to remove? Yes/No

--------------------------------------------

5. Performance inspection

Inspect ONLY.

Report if there are:

- unnecessary deepcopy()
- repeated serialization
- repeated object traversal
- O(n²) loops
- memory leaks

Do NOT optimize anything yet.

--------------------------------------------

6. Produce a final report

Output:

✅ Phase 3.2 status

✅ Phase 3.3 status

Files modified

Tests executed

Dead code report

Performance report

Recommended next task

IMPORTANT:

Do NOT start Phase 3.4.

Do NOT change APIs.

Do NOT change snapshot format.

Do NOT refactor working code.

Only verify, clean temporary debugging code, and report.