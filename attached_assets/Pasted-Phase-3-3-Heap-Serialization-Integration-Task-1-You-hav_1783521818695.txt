Phase 3.3 — Heap Serialization Integration (Task 1)

You have already inspected the project and identified the root cause.

Context

This project is a Python execution tracer.

Phase 3.2 is already completed and working.

The tracer currently supports:

line tracing
call/return events
exceptions
stdout capture
execution limits
snapshots

Phase 3.3 adds heap/object graph serialization.

The goal is that snapshots correctly represent complex Python objects using references and a per-snapshot heap.

The inspection already found the following issues:

_create_snapshot() still uses the old Phase 3.2 serializer (serialize_value) instead of the heap-aware serializer.
_serialize_value_with_heap() immediately returns "unsupported" before any heap logic executes.
_serialize_value_with_heap() references oid without ever assigning it.
_heap_objs is therefore never populated.
snapshot.heap remains empty for every snapshot.
Heap materialization logic exists but is effectively dead code.

The design is correct.

The implementation is simply not connected.

Your task

Implement ONLY the integration fix.

Do NOT attempt to finish all of Phase 3.3.

Do NOT refactor.

Do NOT redesign the tracer.

Do NOT clean up duplicate helpers yet.

Do NOT modify tests.

Keep changes as small as possible.

Part 1 — Fix _serialize_value_with_heap()

Locate:

backend/tracer.py

Find:

_serialize_value_with_heap(...)

Current issue:

It immediately returns

{"type": "unsupported"}

for every non-primitive object.

This prevents every line below from executing.

Fix

Preserve primitive handling exactly as it already works.

For non-primitives:

Assign

oid = id(value)

before any lookup.

Then allow the existing heap/reference logic to execute.

Do NOT rewrite the algorithm.

Only make the existing implementation reachable.

Part 2 — Connect heap serialization

Inside

_create_snapshot()

the locals are currently built using

serialize_value(...)

This is the old Phase 3.2 serializer.

Replace only those calls with

self._serialize_value_with_heap(...)

Do the same for globals.

Do NOT change:

snapshot format
Snapshot dataclass
trace events
execution order
stdout
timing
exception handling
Part 3 — Heap population

After locals/globals are serialized,

the existing loop that materializes

self._heap_objs

should naturally execute.

Do NOT rewrite it unless absolutely necessary.

The objective is simply that:

_obj_id_map gets populated
_heap_objs gets populated
snapshot.heap is no longer empty

using the existing architecture.

Part 4 — Backward compatibility

Everything from Phase 3.2 must continue working.

Do NOT modify:

execution limits
recursion handling
step limits
stdout capture
API routes
models
request/response schema

No behavioral regressions.

Part 5 — Testing

Run ONLY

python backend/test_phase33.py

Do NOT run any other tests.

Report

After running the tests, stop immediately.

Report:

Which tests now pass.
Which tests still fail.
For every remaining failing test:
expected behavior
actual behavior
precise root cause
Show the exact code changes you made.

Do NOT continue implementing further fixes.

Wait for my approval before proceeding to the next Phase 3.3 task.